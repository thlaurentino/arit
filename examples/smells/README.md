# Simple Code Smell Examples

Each file in this directory contains a small, canonical example of a code smell.

To analyze all examples:

```bash
./arit --format text examples/smells/
```

These files intentionally contain problematic examples. They do not represent production code.
